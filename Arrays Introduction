#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */   
    int _n;
    cin >> _n;
    int arr[_n];

    int i = 0;
    while(std::cin >> arr[i++]);
    while(std::cout< <arr[--_n] << ' ' && (_n != 0));

    return 0;
}
